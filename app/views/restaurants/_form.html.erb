<%@restaurators = Restaurator.where(:clientType => 'restaurator')%>

<%= form_for(@restaurant) do |f| %>
  <% if @restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% @restaurant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group">
     <%= f.label :name, 'Nom' %><br />
     <%= f.text_field :name, :class=>'form-control', :placeholder=>'Entrez le nom du restaurant' %>
   </div>
   <div class="form-group">
     <%= f.label :address, 'Adresse' %><br />
     <%= f.text_field :address, :class=>'form-control', :placeholder=>'Entrez l\'adresse du restaurant' %>
   </div>
    <div class="form-group">
      <%= f.label :linkedRestaurator, 'Restaurateur assigné' %><br />
      <%= f.collection_select :linkedRestaurator, @restaurators,:id, :name, :prompt => 'Assignez un restaurateur' %>
    </div>
   <div class="form-group">
     <%= f.submit 'Soumettre', :class=>'btn btn-primary' %>
   </div>
<% end %>
